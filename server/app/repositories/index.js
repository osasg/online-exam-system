const accountRepository = require('./account.repository');
const examRepository = require('./exam.repository');
const subjectRepository = require('./subject.repository');
const examHistoryRepository = require('./exam-history.repository');

module.exports = {
  accountRepository,
  examRepository,
  subjectRepository,
  examHistoryRepository
}
